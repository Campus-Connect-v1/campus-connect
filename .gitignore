# macOS
.DS_Store
**/.DS_Store

# Environment variables (defense in depth; app/, client/, server/ each also ignore these)
.env
.env.local
**/.env
**/.env.local

# Firebase service-account keys. These are send-authority for push and
# must never reach the repo, whatever they are named.
*firebase-adminsdk*.json
*service-account*.json
